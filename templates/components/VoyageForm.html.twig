<div {{ attributes }}>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {# 
    {{ component('Button', {
        formnovalidate: true,
        class: "px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-green-600 hover:bg-green-700"
    }) }} #}
    <twig:Button formnovalidate variant="success" >
        {{ button_label|default('Save') }}
    </twig:Button>

    {{ form_end(form) }}
</div>